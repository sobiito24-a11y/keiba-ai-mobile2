# keiba_data-62 AI展開予測・コース情報監査

## 1. 結論

`keiba_data-62.html` の下部には、完全な実値、会員状態による部分実値、ダミー画像だけの領域が混在している。保存HTMLだけを根拠にすると、10項目の分類は次のとおりである。

| 項目 | 総合分類 | 今回確認できた実値 | 主な所在 |
|---|---|---|---|
| コース条件 | **安定取得** | `新潟1200m芝 Aコース` | 静的HTMLテキスト |
| ペース判定 | **安定取得** | `H`（クラスも `Pace_H`） | 静的HTMLテキスト＋class属性 |
| 推定各馬位置 | **条件付き取得** | 4コーナー・無補正の18頭分は静的座標。ほかの条件はインラインJS内 | `id`、`style=top/left`、インラインJS |
| 4コーナーポジション別複勝率 | **安定取得** | 3×3の9値を全て確認 | 静的HTMLテキスト。行列意味はDOM順・HTMLコメント |
| 有利位置ラベル | **安定取得** | `中目有利` | 静的HTMLテキスト |
| 推定ポジション有利馬 | **条件付き取得** | 6番 セリエンクーニゲンを1頭確認 | 静的HTML。ただし直後に会員向けダミー領域 |
| トラックバイアス | **取得不能** | 実値なし | 空DOM＋ダミー画像。値を表すclassも削除済み |
| AI見解 | **条件付き取得** | 冒頭文のみ。2文目が `...` で切れている | 静的HTMLの部分文＋ダミー画像 |
| 前半/後半3F | **条件付き取得** | 1番のみ `34.3 / 36.3`。残り17頭はなし | 静的table 1行＋ダミー画像 |
| ラップ予測 | **取得不能** | 200/400/600/800mの見出しのみ。数値は0件 | 静的tableの各値セルがダミー画像 |

したがって、現時点で独立した「展開/コース材料」の候補にできるのは、コース条件、ペース、4コーナー複勝率、有利位置ラベルである。推定位置、有利馬、AI見解、前後半3Fは取得完全性を明示できる場合だけ候補になる。トラックバイアスとラップ予測は、この保存物からは利用できない。

**能力コア・能力帯へ入れるべき項目は0件である。** これらはタイム指数由来の純粋なVer3能力とは別の、レース当日の展開・コース材料または提供元AIの派生情報である。

## 2. 対象と判定基準

- 入力: `keiba_data-62.zip` 内の `keiba_data-62.html`
- サイズ: 694,178 bytes
- SHA-256: `06078928aeea6d6ef4f3c007ee51df6152fe221c3658335259dc1446682c60cb`
- canonical / og:url: `https://race.netkeiba.com/race/newspaper.html?race_id=202604020612`
- ページ: JRA、2026年8月9日、新潟12R、3歳未勝利
- 保存状態: `isFreemium`、`MasterPush`、各種dummy画像を含む非マスター表示

分類基準は以下のとおりとした。

- **安定取得**: 対象の実値が静的HTMLに完全なテキストまたは明示属性として存在し、ダミーを値と誤認しない。
- **条件付き取得**: 一部だけ実値、会員状態依存、インラインJavaScriptの実装構文依存、またはリストの完全性を証明できない。
- **取得不能**: ラベルや見出しはあるが実値がなく、空要素またはダミー画像しかない。

監査では保存HTMLとリポジトリ内コードだけを使用した。外部通信、会員表示の推測、ダミー画像からの値の推定は行っていない。

## 3. 項目別の所在と実値

### 3.1 コース条件 — 安定取得

- HTML 1731–1732行付近の `.CourseDataArea.Time .CourseDataTitle h2`
- 静的テキスト: `コース情報 新潟1200m芝 Aコース`
- 会場、距離、馬場種別、コース区分を1つの見出しから取得できる。
- JavaScript変数、画像、`data-*` 属性への依存はない。

将来利用する場合は、表示材料の対象条件キーとして有用。ただし能力指数の入力にはせず、レースIDおよびJRA/NAR modeとの一致確認に使うのが妥当である。

### 3.2 ペース判定 — 安定取得

- HTML 1737–1738行付近
- 要素: `<div class="Data Pace_H NoCheckData">H</div>`
- テキスト `H` とclass `Pace_H` が一致する。AI見解の冒頭にも「ハイペース」があり、今回のHTML内では矛盾しない。
- 値は静的HTMLであり、JavaScript変数ではない。

将来利用する場合は「展開材料」に限定する。提供元AIの予測であり、純能力や能力帯の根拠にはしない。

### 3.3 推定各馬位置 — 条件付き取得

保存時のアクティブタブは `Corner03`（4コーナー）で、`.DevelopImgWrap` に18頭すべての初期DOMがある。

- 馬の対応: `span.HorseIcon` の `id=Horse1` 〜 `Horse18`、馬番、短縮馬名、馬DBリンク
- 位置: inline `style` の `top` / `left`（%）
- 伸び方: 子spanの `SpeedUp_01` / `_02` / `_03`。説明領域には「少し伸びる／伸びる／大きく伸びる」の3段階があるが、class番号との対応は保存HTML内に明示されない。

今回の4コーナー・無補正の生座標は次のとおり。

| 馬番 | 表示名 | top | left | 伸びclass |
|---:|---|---:|---:|---|
| 1 | インザ | -4% | 36.15% | なし |
| 2 | ウィズ | 12% | 58.8% | `SpeedUp_01` |
| 3 | フレア | -4% | 67.18% | `SpeedUp_02` |
| 4 | シエラ | 12% | 80.17% | `SpeedUp_02` |
| 5 | ロジシ | 30% | 93.87% | `SpeedUp_01` |
| 6 | セリエ | -4% | 0% | `SpeedUp_03` |
| 7 | ルナー | 46% | 39.38% | `SpeedUp_03` |
| 8 | タイセ | 64% | 79.9% | なし |
| 9 | サンロ | 46% | 90.77% | `SpeedUp_01` |
| 10 | オンナ | 64% | 51.79% | `SpeedUp_03` |
| 11 | ハルカ | -4% | 54.68% | `SpeedUp_02` |
| 12 | ミクロ | 12% | 46.3% | `SpeedUp_02` |
| 13 | エンジ | 64% | 31.72% | `SpeedUp_03` |
| 14 | ココソ | -4% | 100% | `SpeedUp_02` |
| 15 | ルージ | 12% | 13.36% | `SpeedUp_03` |
| 16 | ラルル | 30% | 55.02% | `SpeedUp_03` |
| 17 | ジェム | 30% | 35.87% | なし |
| 18 | シルバ | -4% | 19.52% | `SpeedUp_02` |

HTML 1511行付近の `updateHorsePosition()` には、次の12組の座標が命令文として直書きされている。

- 条件4種: 無補正、出遅れ率、騎手傾向、出遅れ率＋騎手傾向
- 地点3種: スタート後、3コーナー、4コーナー
- 18頭 × 12組 = 216座標更新

これはJSONや宣言的なJavaScript変数ではなく、`$("#HorseN").css({...})` というUI実装コードである。さらに保存HTMLの3チェックボックスは `id=""` で、JSが参照する `#dev_check_01_02` / `#dev_check_01_03` と一致しない。したがって、初期4コーナー座標は安定して読める一方、全条件をJS構文解析してデータ化する方法は壊れやすく、全体分類は「条件付き」とした。

`top` / `left` の意味を内・中・外、前・中・後へ変換する明示データもない。背景/CSSの見た目を根拠に勝手な意味付けをしてはならず、生座標と取得条件のまま保持すべきである。

### 3.4 4コーナーポジション別複勝率 — 安定取得

- HTML 1917行付近の `dl.PositionMapImg`
- `.PositionMarkList` に9個の静的パーセント値
- 行は前・中・後、列は内・中・外。意味はDOM順とHTMLコメントに記録されている。
- 集計注記: `※集計期間：直近1年`

| 4角位置 | 内 | 中 | 外 |
|---|---:|---:|---:|
| 前 | 33% | 80% | 0% |
| 中 | 13% | 17% | 0% |
| 後 | 5% | 14% | 0% |

数値は完全だが、母数、対象レース数、信頼区間は保存HTMLにない。表示材料にはできても、加点量を直接決める統計としては情報不足である。

### 3.5 有利位置ラベル — 安定取得

- `dl.PositionMapImg > dt` の静的テキスト: `中目有利`
- 画像やJavaScriptに依存しない。

これは上記ヒートマップから派生した要約と考えられる。同じ材料としてまとめ、複勝率行列と別々に二重加点してはならない。

### 3.6 推定ポジション有利馬 — 条件付き取得

- HTML 1960行付近の `.PositionPickupHorseWrap li`
- 実値: 馬番6、セリエンクーニゲン、horse ID `2023110025`
- 静的テキスト、馬番class、馬DBリンクから取得できる。
- 直後に `DummyBox02 MasterPush` があり、非マスター表示では候補リストが完全か判定できない。

よって「少なくとも6番が表示候補」は確定できるが、「有利馬は6番だけ」とは断定できない。推定位置とヒートマップを合成した派生結果の可能性が高く、元情報と同時に別加点しない。

### 3.7 トラックバイアス — 取得不能

値らしい領域は2か所あるが、どちらにも実値がない。

1. 位置図の `.DevelopImg01`
   - classは `Turf AntiClockwise` のみで、コメントに説明される `BiasPattern123` 等は付与されていない。
   - `.DevelopBiasTxt` の3つのspanはclassもテキストも空。
   - `hideTrackBias()` は非マスター時に `BiasPattern*` と3つのテキストを消す。
2. `.CourseDataArea.Bias`
   - 見出し `バイアス` 以外の本文は `pase_dummy.png` だけ。
   - `data-id="master_info_new_trackBias"`、`data-theme="19105"` は課金UI識別子で、バイアス値ではない。

別領域にある当日傾向のニュース文などを代替値として混ぜてはならない。この保存HTMLからのトラックバイアス取得は0件である。

### 3.8 AI見解 — 条件付き取得

静的HTMLに次の冒頭だけ存在する。

> ハイペースが予想される今回のレースでは、終盤の伸び脚が鍵を握る展開となりそうです。  
> 6.セリエンクーニゲンは3コーナ...

2文目はHTML自体が `...` で切れており、その直後に `comment_dummy.png` がある。これは完全な見解ではない。保存後のJavaScript変数やDOM属性にも続きはないため、冒頭の表示用参考文としてのみ取得できる。文の続きを補完・要約してはならない。

### 3.9 前半/後半3F — 条件付き取得

- HTML 1700行付近の `table.PredictRap_Table`
- 1行目だけ実値: 1番 インザライムライト、前半3F `34.3`、後半3F `36.3`
- 次行は `table_dummy_01.png`、`table_dummy_02.png`、`table_dummy_03.png` で、残り17頭の数値は存在しない。
- コース全体の「推定前半3F」「推定後半3F」（1745–1750行付近）も `pase_dummy.png` だけ。

したがってカバレッジは `1/18`。全頭比較や順位化には使えない。

現行JRA parserは、表の「馬番」「前半3F」「後半3F」という列名を一般的に探索するため、この1頭分を既に読み取る。ただし `build_jra_late3f_materials()` は後半3Fの有効数が3頭未満なら全材料を空、点数を0にする。実際に本HTMLを現行parserへ通した結果は、抽出18頭、有効な予測3F 1頭、生成された末脚材料0頭、材料点合計0.0だった。

### 3.10 ラップ予測 — 取得不能

- HTML 1304行付近の `table.Race_HaronTime`
- 見出しは `200m / 400m / 600m / 800m`
- 値行の4セルはすべて `pase_dummy.png`
- 数値テキスト、`data-*` 属性、JavaScript変数、JSON payloadはない。

距離見出しは取得できるが、予測ラップ値は0件である。画像OCRを行っても元画像は共通ダミーなので値にはならない。

## 4. 監査開始時点のParser・能力独立性への影響

この節は実装前監査時点の記録である。コード追跡で確認した境界は次のとおり。

1. `core/ver3_ability.py` の `calculate_ver3_ability_core()` は、近3走平均、★最高、近3走最高、前走、距離、コースの6指数だけを引数に取る。
2. JRAの能力コアは `core/jra_notebook_logic.py` 1950行付近で計算され、`_ver3_ability_core` に保存される。
3. 競馬新聞HTMLの追加処理はその後に行われる。`parse_jra_newspaper_html()` が今回のAI領域から偶然/汎用的に認識するのは、列見出しが一致する「前半/後半3F」だけである。
4. 読み取られた予測3Fは `末脚材料` / `_新聞材料点` の候補であり、`_ver3_ability_core` の再計算には渡されない。値が揃った場合に最終印の補助材料へ入る既存経路はあるが、能力コア・能力帯とは別である。
5. コース見出し、ページのペース `H`、位置座標、複勝率行列、有利位置ラベル、有利馬、トラックバイアス、AI見解、ラップ予測を直接読む現行parser selector/keywordは見つからなかった。

特に、今回のページに表示された `H` は現在のアプリの独自展開判定へ入力されていない。将来これを使う場合も、既存の展開分析と同じ「展開/コース材料」層で出典を分ける必要がある。

## 5. 将来の独立材料としての評価

| 項目 | 独立材料としての評価 | 主な注意 |
|---|---|---|
| コース条件 | 高 | 条件キー/表示用。能力点にはしない |
| ペース判定 | 中〜高 | 提供元AI予測。独自ペース判定と重複管理が必要 |
| 推定各馬位置 | 中 | 完全性と座標意味を検証。JS構文依存を避ける |
| 4角複勝率 | 中〜高 | 母数なし。行列値と要約ラベルを二重加点しない |
| 有利位置ラベル | 中 | 行列の派生要約として同一材料群に束ねる |
| 推定ポジション有利馬 | 中 | 位置×行列の派生結果。完全リストか不明 |
| トラックバイアス | 不可 | 今回は実値なし |
| AI見解 | 低 | 不完全かつ不透明。ほかのAI項目を言い換えた可能性 |
| 前半/後半3F | 中〜高（全頭時） | 今回は1/18。全頭カバレッジ必須 |
| ラップ予測 | 不可 | 今回は実値なし |

ペース、位置、予測3F、有利馬、AI見解は、同じAI展開予測から派生した相関の強い情報群である可能性が高い。将来実装する場合も、5項目を独立の加点要素として積み上げず、1つの「提供元AI展開材料」グループとして表示・上限制御するべきである。4角複勝率、有利位置ラベル、有利馬にも「集計値→要約→該当馬」という派生関係があり、同様に二重・三重評価を避ける。

最低限必要な取得条件は以下である。

- `race_id` とJRA/NAR modeが他の入力HTMLと一致する。
- `isFreemium`、`MasterPush`、`DummyBox*`、`*_dummy.png` を欠損の証拠として扱う。
- 項目ごとに `coverage`（例: `1/18`）と `complete=false` を保持する。
- 静的DOM、インラインJS、派生ラベルの出典を別々に記録する。
- 欠損値を推測・0・平均値で補わない。
- これらから生成する表示や点数を、`_ver3_ability_core`、能力評価値、AA/A/B/C/Z能力帯へ渡さない。

## 6. 補助的な再現性確認

既存fixtureの七夕賞 `keiba_data-61.html` と構造を比較したところ、コース見出し、`Pace_*`、18/16頭の位置DOM、3×3ヒートマップ、有利位置ラベル、有利馬のselectorは共通だった。一方、両HTMLとも予測3Fは1頭だけ、ラップは4セルすべてダミー、トラックバイアスは空だった。

これは静的selector候補の再現性を補強するが、2例ともJRA・非マスター保存である。NAR、マスター表示、障害戦、取消/除外、少頭数、ダート/直線コースへの一般化は未確認であり、別fixtureなしに「全ページで安定」とは結論しない。

## 7. 監査後の実装結果

- `core/course_materials.py` を追加し、競馬新聞HTMLから明示実値だけを構造化する。
- JRA/NAR共通でコース条件、提供元ペース、スタート後/3角/4角位置、4角複勝率行列、有利位置ラベル、表示済み有利馬、AI見解の完全性、予測3Fカバレッジ、バイアス/ラップの取得状態を保持する。
- NAR保存HTML内のコメント済み旧馬群座標は除外し、実DOMの馬番集合でフィルタする。
- `isFreemium`、`MasterPush`、`DummyBox*`、dummy画像を検出し、部分値を完全値へ昇格しない。
- Market Compareでは提供元ペース、位置、傾向、有利馬を1つの「展開/コース材料」に束ねる。能力値、能力順位、AA/A/B/C/Z、適正オッズには渡さない。
- 騎手コース率は今回の保存HTML群に実値なし。新潟HTMLの `AnaBestTable` にある上位候補だけを「率・件数なしの参考順位」として保持し、強評価しない。
- Ver3/Ver4/実戦モードはこの追加parserを呼ばず、Market Compareモードだけが独立表示用に呼ぶ。
- 完全回帰: `python -m pytest -q` → `200 passed, 14 subtests passed`。
- commit / push: 未実施。

詳細な変更・テスト・表示例は `FINAL_PRE_PUSH_AUDIT.md` に記録する。
