# 能力×価格比較モード 監査・設計報告

## 結論

旧Colabの全頭横比較と、現MobileのParser・`PredictionResult`・条件適性・履歴保存を統合し、既存モードを残したまま `market`（能力×価格比較）モードを追加した。

新モードは自動BUY、印、SSを主画面から外し、以下の4軸を分離する。

1. 能力: Ver3のタイム指数合成を基礎に1回だけ評価
2. 市場価格: 実単勝オッズを能力帯内で比較
3. 今日の条件: クラス、間隔、斤量、騎手、距離・コース実績、調教等を＋／－材料として表示
4. 展開: 全頭の脚質構成から独立表示

再送された `keiba_data-61.zip` から七夕賞の実HTMLを取得し、9番オニャンコポンの市場・当日材料と能力独立性を固定する回帰テストを追加した。

## Phase 1: 旧Colab・現Mobile・Parser監査

### 能力計算

現行Ver3は以下のタイム指数を中心に `raw_score` を作り、レース内min/maxで表示用 `AI点`（60〜100）へ変換している。

- 近3走平均
- 条件一致時の★最高指数
- 近3走最高
- 前走指数
- 距離指数
- コース指数

監査で、互換用 `raw_score` には当日斤量補正が含まれ、JRAでは状態・条件由来の小加点も含まれることを確認した。現在は6つのタイム指数だけから作る補正前の値を `_ver3_ability_core` として直接保存し、新モードの能力値・順位・帯はこの列だけから作る。既存Ver3の `raw_score` は変更しない。`raw_score - _market_non_ability_adjustment` は、明示コアを持たない旧保存データだけの互換処理とした。詳細は `ABILITY_INDEPENDENCE.md` に記録した。

### データ取得区分

| 項目 | 区分 | 監査結果・取得元 |
|---|---|---|
| 馬番・馬名・馬齢 | A | JRA/NARタイム指数、出馬表・競馬新聞Parser |
| 人気・単勝オッズ | A | 出馬表・競馬新聞・脚質入力の既存列 |
| タイム指数・近走・距離・コース | A | 現MobileのVer3 Parser |
| 脚質 | A | 脚質分析HTML/JSON。未添付時は未取得表示 |
| 斤量・斤量増減 | A | 現斤量と前走斤量の既存Parser列。差分がない場合は未取得 |
| 騎手・継続/乗替 | A | 現騎手・前走騎手の既存Parser列。前走情報がない場合は未取得 |
| レース間隔 | A | 既存の日付差・表示列 |
| 今回/前走/近3走最高クラス | A | `_current_class_*`、`_previous_class_*`、`_best_past_class_*`、`_past_runs` |
| クラス経験・好走歴 | A | `_past_runs` のクラスと着順を事実照合 |
| ★/☆/※条件適性 | A | Ver4.1で修正済みのcondition plumbingを再利用 |
| 馬場実績 | A | 既存Parser列。入力にない場合は空欄 |
| JRA調教評価・短評 | A/C | 調教HTMLがあればA。未添付・HTMLにない場合は取得不能 |
| 厩舎コメント | A/C | JRA競馬新聞Parserで取得できた場合のみ詳細カードへ表示 |
| 対戦材料 | A/C | 既存列・入力に存在する場合のみ。推測補完しない |
| 旧Colab型の全頭横比較UI | B | 旧Colabでは見やすかったが現Mobile主画面で縮小。新UIで復活 |
| 競馬場×距離の騎手勝率/複勝率 | D | 現入力・Parserに信頼できる全騎手データなし。推測しない |
| AI適正オッズ校正用の全頭ラベル | D | 同梱188Rは各レース本命1頭だけ。全頭勝率を校正できない |

区分: A=現Mobileで取得可能、B=旧Colab/UIに存在、C=HTML・入力次第、D=取得不能または統計不足。

## Phase 2〜5: データモデル・能力帯・材料・UI

### 能力帯

結果を見ずに固定した閾値を使用する。オッズ、人気、騎手、斤量、間隔、展開、＋／－材料は参照しない。

- AA: `market_ability_score >= 95` かつ2位差5以上の首位だけ。条件を満たさなければ0頭
- A: 首位差5以内
- B: 首位差12以内
- C: 首位差20以内
- Z: それより下、または能力材料不足

AAを必ず作らない。Aは複数頭を許容する。帯内は単勝オッズ昇順で表示する。

### 状態矢印

- `↑ 上昇`: 近3走が連続上昇し、最初から前走まで6以上上昇
- `↗ 持ち直し`: 中間走から反発、または緩やかな上向き
- `→ 安定`: 変動幅4以内、または始点と終点の差が小さい
- `↘ 弱含み`: 中間走から低下、または緩やかな下向き
- `↓ 下降`: 近3走が連続下降し、6以上低下
- `？ 判定保留`: 有効指数が2走未満

必ず `54→68→72` のような推移を併記する。

### クラス判定

Parserが持つ今回・前走・近3走最高クラスと `_past_runs` を表示する。今回と同じクラスの履歴があれば「経験あり」、そのクラスで3着以内があれば「好走歴」とする。昇級で同級履歴がなければ「初○○」を－材料にする。取得できない場合は「未取得」「判定保留」とし、推測しない。

### 展開判定

全頭の逃・先・差・追を集計し、次の事実ルールだけを使う。

- 逃1頭かつ前3頭以下: 単騎逃げ候補・スロー寄り
- 逃2頭以上、または逃＋先が既定比率以上: 先行馬多数・平均以上
- 前2頭以下: 前少なめ・スロー寄り
- その他: 平均想定

各馬は `展開○ / ± / －` と理由を持つが、能力値へ加点・減点しない。

### 騎手

騎手名と継続/乗替を表示する。騎手の知名度・人気は＋材料や能力加点に使わない。競馬場×距離成績はデータ未取得のため表示しない。

### ＋／－材料

条件一致実績、状態矢印、クラス経験、間隔、斤量増減、展開、JRA調教の取得事実から重複を除いて列挙する。件数の合計点や最終点は作らない。オッズ・人気は材料リストへ入れない。

## Phase 6: AI適正オッズ

表示は `未校正` とした。

同梱の188Rは各レースの本命1頭について能力帯と勝敗を持つが、全出走馬の能力評価・順位・帯・頭数・JRA/NARと勝敗の組を持たない。この状態で全頭の勝率へ変換すると選択バイアスが入り、development/holdout分割、Brier score、log loss、calibration curveを正しく検証できない。

実装条件は以下。

1. 全頭の予測時点スナップショットを結果前にfreeze
2. 時系列でdevelopment/holdoutを分離
3. race内確率和を検証
4. Brier score、log loss、reliability diagramを先に確認
5. ROIは補助指標に限定

## Phase 7〜8: カード・要約・freeze

- 能力帯×オッズを最上部へ配置
- 全頭横比較表を横スクロールで表示
- 2〜3行要約＋詳細展開カードを実装
- AI要約は能力・市場・展開・変化の4行だけ。購入結論を出さない
- ユーザーが選択馬・理由・券種を自由保存可能
- `prediction.json`、`prediction.sha256`、`result_template.json` を分離保存
- 予測signatureは結果・払戻・着順列を対象外とし、結果列の有無で一致する回帰テストを追加

## Phase 9〜10: 回帰・sanity

追加テストは以下を検証する。

- AAが0頭でも成立
- Aが複数頭でも成立
- 能力帯内をオッズ順表示
- 人気・オッズ・騎手・間隔・斤量表示・展開で能力値/順位/帯が変化しない
- 現行予測は明示したVer3能力コアを直接使用し、旧保存データだけ記録済み非能力調整を差し引く
- 旧raw・旧補正を矛盾する値へ変えても、明示コアが同じなら能力値/順位/帯は不変
- A帯の73.5倍がAのまま残る
- Z帯の500倍が価格で昇格しない
- ＋材料に「買い」「VALUE」を生成しない
- 結果列なし/ありで予測signature一致
- AI適正オッズは未校正表示
- JRA/NARの双方で新モードを評価可能

七夕賞実HTMLsanityでは、9番オニャンコポン、73.5倍、15番人気、差し、中1週、54kg、吉田豊、`B 復調気配`、「もともとの地力がある馬」を固定した。HTMLは競馬新聞ページでタイム指数ページではないため、当日の正確なVer3能力値は捏造せず、実市場値・当日材料と能力コアの独立性を回帰対象にした。

検証結果（2026-08-13）:

- 既存＋追加テスト: `169 passed, 12 subtests passed`
- 能力独立性・能力×価格比較・七夕賞回帰: 16件成功
- 全Pythonファイル `py_compile`: 成功
- `app.py` import smoke: 成功
- JRA/NAR: 既存Parser・予測・新モードの単体/結合回帰は成功
- 実HTML代表fixture: 七夕賞16頭を抽出し、オニャンコポン回帰成功

## 旧Colabから復活したもの

- 全頭横比較表の情報密度
- クラス履歴・根拠
- レース間隔
- 脚質構成と展開
- 状態推移
- 騎手・斤量・調教・コメントの横比較

## 現Mobileから残したもの

- Ver3タイム指数Parserとデータ構造
- `raw_score`と既存AI点（互換列）
- Ver4.1 condition plumbing
- JRA/NAR入力分類
- `PredictionResult`
- SHA-256を使う結果前freeze思想
- 旧Ver3、Ver4、Ver4.1、旧実戦モード

## 主画面から外したもの

- ◎○▲△✓、SS、BUY/LIGHT/WATCH/SKIP
- 自動馬券と期待値誘導ラベル

コード上は互換・研究用として折りたたみ表示に残す。
